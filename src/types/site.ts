export type HeroStat = {
  value: string;
  label: string;
  description: string;
};

export type StarterFeature = {
  label: string;
  title: string;
  description: string;
  metric: string;
};

export type CollectionHighlight = {
  tag: string;
  title: string;
  description: string;
};
